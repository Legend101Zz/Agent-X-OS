"""agentx_operator — the Operator Agent (BLUEPRINT §6.1). NOT Phase 1 — near-term placeholder.

The founder's chief-of-staff: a gated, privileged USER of the kernel's command/query API (never code
inside the live kernel — that would break invariant #4). Its entire tool surface is the dashboard's
own command/query API, so every action it takes is the same journaled, gated command a human clicks.

Empty in Phase 1. Do not implement here during Session B.
"""
