import StubPage from "../_components/stub-page";

export default function ApprovalsPage() {
  return (
    <StubPage
      title="Approvals"
      cardId="C7"
      cardTitle="C7 — Approvals inbox (first-class gate UX: approve / reject / edit+diff)"
      description="L0/L1 inbox. Approve, reject, or edit (with old→new diff). Inline approve from any Inspector."
    />
  );
}