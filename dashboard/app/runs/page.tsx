import StubPage from "../_components/stub-page";

export default function RunsPage() {
  return (
    <StubPage
      title="Runs"
      cardId="C6"
      cardTitle="C6 — Runs & Trace viewer"
      description="Runs across instances with timeline, claimed facts, settlement summary."
    />
  );
}